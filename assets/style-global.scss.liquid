body {
    line-height: 1.45;
    background: var(--shop-bg-color);
    font-family: var(--base-font), sans-serif;
    position: initial;
    font-size: 14px;
}
body,a {color: var(--shop-text-color);}